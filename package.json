{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "devDependencies": {
    "lerna": "^6.4.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "prettier": "^2.8.1",
    "prettier-plugin-tailwindcss": "^0.2.0"
  },
  "scripts": {
    "format": "prettier -w **/*.tsx",
    "run-photography": "npx lerna run dev --scope=portfolio-photography",
    "run-development": "npx lerna run dev --scope=portfolio-development"
  },
  "name": "Portfolio"
}
