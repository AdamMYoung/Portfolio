{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "eslint-config-custom": "*",
    "prettier": "^2.8.1",
    "prettier-plugin-tailwindcss": "^0.2.0",
    "turbo": "^1.9.1"
  },
  "scripts": {
    "format": "prettier -w **/*.tsx",
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo run lint"
  },
  "name": "portfolio",
  "packageManager": "yarn@3.3.0"
}
